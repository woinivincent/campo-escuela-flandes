# Contraseña del panel de administración (/admin).
# Sin esta variable el panel no deja entrar a nadie: no hay clave por defecto.
# En producción se carga en Netlify → Site configuration → Environment variables.
# En desarrollo, copiar este archivo a .env.local y poner una clave cualquiera.
ADMIN_PASSWORD=
